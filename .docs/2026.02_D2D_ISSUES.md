# D2D Player Issues - February 2026

## Status Overview

| Issue | Status | Priority |
|-------|--------|----------|
| Loop crash on #4-5 | FIXED | - |
| Swap chain buffer rotation | FIXED | - |
| WS_EX_TRANSPARENT crash | FIXED | - |
| Only first frame visible (GIF) | FIXED | - |
| GIF centering/positioning | FIXED | - |
| GIF/MP4 animation too slow | OPEN | HIGH |
| GIF doesn't loop | OPEN | HIGH |
| Memory leak in D2D Player | OPEN | HIGH |
| UI freeze on animation switch | OPEN | HIGH |
| LibVLC unnecessary init for GIFs | FIXED | - |
| Horizontal centering broken | FIXED | - |
| Magick.NET extraction slower than GDI+ | FIXED | - |
| Background color from GIF edge pixels | OPEN | MEDIUM |

---

## FIXED Issues

### 1. Player Crash on GIF Loop #4-5
**Root Cause:** `OnMediaEndReached` callback called `_mediaPlayer.Stop()` and `Play()` synchronously from LibVLC event thread.
**Fix:** Removed Stop/Play from callback. Looping now handled only by 95% position seek in `VideoDisplayCallback`.

### 2. DXGI Flip Model Buffer Rotation
**Root Cause:** D2D render target created once, never recreated after `Present()`. Buffers rotate with FlipSequential.
**Fix:** Added `RecreateD2DRenderTarget()` called after every `Present()`.

### 3. Explorer Crash (WS_EX_TRANSPARENT)
**Root Cause:** `WS_EX_TRANSPARENT` crashes explorer.exe on Windows 11 24H2+.
**Fix:** Replaced with `WS_EX_LAYERED + SetLayeredWindowAttributes(0xFF)`.

### 4. Only First Frame Visible (GIF Animation)
**Root Cause:** LibVLC's image demuxer treats GIFs as static images - decodes only frame 0 and re-displays it at max speed. Even with `--demux=avformat`, Duration reports as 100ms and EndReached fires after 1 display callback. Each Stop/Play restart only produces frame 0.
**Proof:** Buffer hash (`88247DB5`) was constant across ALL display callbacks. Position stuck at `0.000`.
**Fix:** GDI+ frame pre-extraction. `ExtractGifFrames()` loads all frames using `SelectActiveFrame()`, JPEG-compresses to `List<byte[]>` (~75MB vs ~4GB raw). `GetFrameAtPosition()` decodes on-demand with single-frame cache. LibVLC still used for MP4/video.
**Additional fixes during investigation:**
- Removed `_frameLock` deadlock (seeking LibVLC while holding lock)
- Removed `input-repeat=-1` (caused rapid restart loops)
- Fixed `OnMediaEndReached` to use `ThreadPool.QueueUserWorkItem`

---

## OPEN Issues

### ISSUE-004: GIF/MP4 Animation Speed Too Slow (HIGH)

**Symptoms:**
- GIF animation plays but noticeably slower than expected
- MP4 video also plays too slow (confirmed working but wrong speed)
- Both media types affected - suggests a common timing/speed multiplier issue

**Suspected Root Causes:**
1. `SpeedMultiplier` from `AnimationLayerConfig` is not applied to GIF frame timing in `GetFrameAtPosition`
2. For MP4: `SpeedMultiplier` is not applied to `_mediaPlayer.Rate` in LibVLC
3. The elapsed time source (`timestampMs`) may be running slower than real-time

**Key Files:**
- `VideoWallpaperRenderer.cs` - `GetFrameAtPosition()` GIF timing, LibVLC rate
- `AnimationLayerRenderer.cs` - `UpdatePosition()`, `_currentElapsedMs`
- `WallpaperConfig.cs` - `SpeedMultiplier` property
- `D2DPlayerHost.cs` / `Program.cs` - timestamp calculation in render loop

---

### ISSUE-005: GIF Doesn't Loop (HIGH)

**Symptoms:**
- GIF plays through once and stops at last frame
- Render loop continues (Frame #300+) but image is static
- Logs show `Playing: True` and `ShouldCompose: True` - composition keeps running

**Suspected Root Causes:**
1. `timestampMs` passed to `GetFrameAtPosition()` keeps increasing past `_gifTotalDurationMs` - the modulo should handle this, but need to verify
2. The frame index calculation may be stuck on last frame when `accumulated` never exceeds `loopedMs`
3. The elapsed time might overflow or the modulo might not work as expected

**Key Files:**
- `VideoWallpaperRenderer.cs` - `GetFrameAtPosition()` GIF looping logic
- `AnimationLayerRenderer.cs` - `_currentElapsedMs` passed as `timestampMs`
- `Program.cs` - render loop timestamp source

---

### ISSUE-006: GIF Frame Extraction Slow - FIXED (2026-02-07)

**Original Problem:** GDI+ `SelectActiveFrame()` took ~8s. First Magick.NET attempt was even slower (41875ms) because JPEG encoding 500 frames dominated the time.

**Fix:** Eliminated JPEG encode/decode entirely. Changed `VideoWallpaperRenderer.ExtractGifFrames()` from storing `List<byte[]>` (JPEG-compressed) to `Bitmap[]` (direct `ToBitmap()` from Magick.NET). This removes:
- JPEG encoding during extraction (the bottleneck - 500 frames x ~80ms each)
- JPEG decoding during playback in `GetFrameAtPosition`
- Single-frame decode cache (`_cachedGifFrameIndex` / `_cachedGifFrame`) - no longer needed

**Trade-off:** Higher memory usage (raw Bitmap ~800KB/frame vs JPEG ~70KB/frame), but much faster extraction and zero-overhead playback.

**NuGet packages:**
- `Magick.NET-Q8-AnyCPU` 14.10.2
- `Magick.NET.SystemDrawing` 8.0.15
- `System.Drawing.Common` upgraded to 8.0.22

---

### ISSUE-007: Memory Leak in D2D Player (HIGH)

**Symptoms:**
- Player.D2D.exe memory grows continuously during playback
- Memory keeps growing even after GIF animation stops at last frame
- Render loop continues composing frames even though visual output is static

**Suspected Root Causes:**
1. `GetFrameAtPosition()` returns `new Bitmap(...)` clones - caller may not dispose them
2. `ComposeForScreen()` creates bitmaps that may not be disposed after D2D conversion
3. `ConvertBitmapToD2D()` may leak D2D bitmaps
4. The composition pipeline creates intermediate bitmaps each frame without disposal

**Key Files:**
- `VideoWallpaperRenderer.cs` - `GetFrameAtPosition()` bitmap cloning
- `CompositionRenderer.cs` - `ComposeForScreen()` bitmap lifecycle
- `AnimationLayerRenderer.cs` - `RenderForScreen()` bitmap creation
- `Program.cs` - render loop bitmap handling, D2D bitmap disposal

---

### ISSUE-008: GIF Centering/Positioning - FIXED (2026-02-07)

**Fix Applied:**
- Added `ContentFitMode` enum (Stretch/Center/Fit/Fill) to `AnimationLayerConfig` in `CrossScreenConfig.cs`
- Added `ContentWidth`/`ContentHeight` to `IWallpaperRenderer` interface, implemented in all 3 renderers
- Rewrote `CalculateAnimationDimensionsAsync()` with FitMode-based logic using actual content dimensions
- Updated `RenderForScreen()` with proper source/dest rectangle mapping
- Added ComboBox to `MainWindow.axaml` and `SelectedFitModeIndex` to ViewModel

**Remaining Issue:** See ISSUE-009 - horizontal centering is broken (vertically centers correctly but NOT horizontally)

---

### ISSUE-002: UI Freeze on Animation Switch (HIGH)

**Symptoms:**
- Clicking "Apply with Direct2D" a second time freezes the application
- Background turns black, UI becomes unresponsive
- Log shows: `Playing: False | ShouldCompose: False`

**Suspected Root Causes:**
1. `SendStartAnimationAsync()` never called after second `SendLoadAnimationAsync()`
2. Disposing old composition blocks on UI thread
3. Async IPC protocol race condition on reload

**Key Files:**
- `D2DPlayerHost.cs` - reload sequence
- `MainWindowViewModel.cs` - Apply button handler

---

### ISSUE-009: Horizontal Centering Broken in Center FitMode - FIXED (2026-02-07)

**Root Cause:** `InitializeAsync()` set `_currentVirtualX = 0` for centered content - left-aligned instead of centered. Vertical centering worked because `CalculateVerticalPosition()` used `_virtualCanvasHeight`, but there was no equivalent horizontal calculation.

**Fix:** Added `_screenWidth` field, stored during `CalculateAnimationDimensionsAsync()`. Changed centering to `_currentVirtualX = (_screenWidth - _animationWidth) / 2`. Works for all FitModes: Stretch gives offset 0 (correct), Center/Fit give positive offset (centered), Fill gives 0 or negative (extends beyond screen edges).

---

### ISSUE-010: LibVLC Unnecessary 10-Second Init for GIFs - FIXED (2026-02-07)

**Root Cause:** `InitializeAsync()` always initialized LibVLC (~10s) even for GIF files that use Magick.NET extraction.

**Fix:** Check file extension + `HeadlessMode` at start of `InitializeAsync()`. For `.gif` files in headless mode, set `_useMemoryCallbacks = true` and return immediately - skipping LibVLC entirely. `StartAsync()` already routes to `ExtractGifFrames()` for this case.

---

### ISSUE-011: Background Color From GIF Edge Pixels / User-Selectable (MEDIUM)

**Symptoms / Feature Request:**
- Currently background behind centered GIF is always black
- User wants option to:
  1. Auto-detect background color from GIF's outer edge pixels (pre-selected suggestion)
  2. Allow user to select a static background color manually
- This is important for GIFs that have a non-black background - black border looks jarring

**Implementation Notes:**
- Need to sample edge pixels of first GIF frame (top row, bottom row, left column, right column)
- Calculate dominant/average color from these pixels
- Use as default suggestion in UI color picker
- Need new UI controls area for background settings (color picker, "auto from edges" toggle)
- Background color should be passed to `BackgroundLayerRenderer` or composition pipeline

**Key Files:**
- `AnimationLayerRenderer.cs` or `BackgroundLayerRenderer.cs` - edge pixel sampling
- `AnimationLayerConfig` / `BackgroundLayerConfig` - background color property
- `MainWindow.axaml` - new UI controls section for background settings
- `MainWindowViewModel.cs` - color picker binding

---

## Architecture Notes

### Render Pipeline Flow (Current):
```
GIF Path (VideoWallpaperRenderer):
  Magick.NET MagickImageCollection.Coalesce() → JPEG byte[] storage
    ↓ GetFrameAtPosition(timestampMs)
  Decode JPEG → Bitmap (cached by frame index)

GIF Path (GifWallpaperRenderer - headless=false):
  Magick.NET MagickImageCollection.Coalesce() → Bitmap[] cache via ToBitmap()
    ↓ GetFrameAtPosition(timestampMs)
  Return cached Bitmap clone

MP4/Video Path:
  LibVLC VideoDisplayCallback → _currentFrameBitmap
    ↓ GetFrameAtPosition(timestampMs)
  Clone bitmap

Both paths:
  AnimationLayerRenderer.RenderForScreen()
    ↓ Crops to screen viewport with FitMode (Stretch/Center/Fit/Fill)
  CompositionRenderer.ComposeForScreen()
    ↓ Composites background + animation
  D2DPlayer.ConvertBitmapToD2D()
    ↓ GDI+ Bitmap → D2D bitmap
  D2DPlayer._d2dRenderTarget.DrawBitmap()
    ↓ Draws to render target
  _swapChain.Present()
    ↓ Presents to screen
  RecreateD2DRenderTarget()
    ↓ Rebinds to new back buffer
```
